<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mayo.godko" >

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".ActMain"
            android:label="@string/app_name" >

        </activity>
        <activity
            android:name=".ActGodHouseDetails"
            android:label="@string/title_activity_act_home" >
        </activity>
        <activity
            android:name=".ActCheckout"
            android:label="@string/title_activity_act_checkout" >
        </activity>
        <activity
            android:name=".ActReligions"
            android:label="@string/title_activity_act_religions" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
